@model IEnumerable<Faps.Models.Vagas>

@{
    ViewBag.Title = "Home_vagas";
    Layout = "~/Views/_FapsPage.cshtml";
}

<h2>Home_vagas</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Codigo_vaga)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vaga)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Codigo_vaga)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vaga)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }

</table>

Gosto de alguma vaga?
<a href="#" class="w3-button w3-black">Faça seu login agora!</a>

Não tem uma conta?
<a href="#" class="w3-button w3-black">Cadastre seu perfil agora!</a>
